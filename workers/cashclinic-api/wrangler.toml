name = "cashclinic-api"
main = "src/index.ts"
compatibility_date = "2025-09-01"

[vars]
ALLOWED_ORIGIN = "https://statecraft.systems"

# Secrets (set with: wrangler secret put ANTHROPIC_API_KEY):
# ANTHROPIC_API_KEY — Claude API key for provisional report generation
